const Footer=(props)=>{
    return(
        <footer className="bg-cyan-800 p-5 font-medium text-white">
            <p>{props.stand.length} Locations World Wide </p>
        </footer>
    )
}

export default Footer