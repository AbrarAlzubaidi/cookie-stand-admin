const Footer=(props)=>{
    return(
        <footer className="p-5 font-medium text-white bg-cyan-800">
            <p>{props.report.length} Locations World Wide </p>
        </footer>
    )
}

export default Footer